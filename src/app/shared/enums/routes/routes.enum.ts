export enum ERoutes {
  HOME = '',
  AUTH = 'auth',
  REGISTER = 'register',
  LOGIN = 'login',
  CONFIRM = 'confirm',
  SELECT = 'select',
  SUCCESSFULLY = 'successfully',
  RESET_PASSWORD = 'reset',
  SEND = 'send',
  UPDATE = 'update',
  CATALOG = 'courses',
  COURSE = 'course'
}
